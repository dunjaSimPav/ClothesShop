@model Cart

@inject Localizer L

@if (Model.Lines.Count() > 0) 
{
    <li class="nav-item">
        <a class="nav-link" href="/Cart">
            <i class="fa fa-shopping-cart"></i> @Model.Lines.Sum(x => x.Quantity) @L["item(s)"] (@($"{Model.ComputeTotalValue()} RSD"))
        </a>
    </li>
}